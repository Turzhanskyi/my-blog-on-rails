en:
  activerecord:
    models:
      post: "Posts"
    attributes:
      post:
        title: "Title"
        summary: "Summary"
        body: "Content of post"
        all_tags: "All tags"
        category_id: "Category_id"
        category: "Category"
      category:
        name: "Name"
        parent_id: "Parent category"

    errors:
      messages:
        blank: "- can't be blank"
        record_invalid: "Errors occurred: %{errors}"
        restrict_dependent_destroy:
          has_one: "Unable to delete record because dependency exists: %{record}"
          has_many: "Unable to delete record because dependencies exist: %{record}"
